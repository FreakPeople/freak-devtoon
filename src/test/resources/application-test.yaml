spring:
    application:
        name: devtoon
    config:
        activate:
            on-profile: test
    datasource:
        url: jdbc:mysql://${TEST_MYSQL_URL}
        username: ${TEST_MYSQL_USER}
        password: ${TEST_MYSQL_PASSWORD}
    jpa:
        properties:
            hibernate:
                show_sql: true
                format_sql: true
        show-sql: true